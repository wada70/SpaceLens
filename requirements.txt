# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
jinja2==3.1.4
aiofiles==24.1.0

# Settings
pydantic-settings==2.6.1

# Confluence
httpx==0.27.2

# LLM
anthropic==0.40.0

# Semantic re-ranking
sentence-transformers==3.3.1
torch==2.5.1          # CPU-only build is fine; override in Docker if GPU available

# Dev / test
pytest==8.3.3
pytest-asyncio==0.24.0
httpx                  # already listed above, used in TestClient too
